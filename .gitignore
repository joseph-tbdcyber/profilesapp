# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.npm
dist
dist-ssr
*.local

# SAM build artifacts
.aws-sam

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# amplify
.amplify
amplify_outputs*
amplifyconfiguration*

# Work in progress: the post-confirmation trigger is half-written and fails the
# Amplify backend typecheck, which fails the whole build. Kept local until it is
# finished. Remove this line to start tracking it again.
amplify/auth/post-confirmation/
